// public static class Helpers
// {
//     public static float CalculateDistance(Vector3 pointA, Vector3 pointB)
//     {
//         return Vector3.Distance(pointA, pointB);
//     }
//     public static Vector3 GetRandomDestination(Vector3 minBounds, Vector3 maxBounds, string areaType)
//     {
//         // Generate random coordinates within the specified bounds
//         float randomX = Random.Range(minBounds.x, maxBounds.x);
//         float randomY = Random.Range(minBounds.y, maxBounds.y);
//         float randomZ = Random.Range(minBounds.z, maxBounds.z);

//         return new Vector3(randomX, randomY, randomZ);
//     }
// }
